/* Inventory menu bridge v6.0: enterprise inventory detail + unified logo, 2026-08-21. */
(function(){
  let root=null,host=null,current='overview',inventorySession=0;
  const sections=[['overview','재고현황'],['movement','입출고 관리'],['lot','LOT별 재고'],['production','생산투입/완료'],['count','재고실사']];
  const clean=v=>String(v||'').replace(/\s+/g,' ').trim();
  const upper=v=>clean(v).toUpperCase();
  const NAMO_LOGO='/logo.png';
  function iqcRows(){const db=window.DB||{};return [db.iqc,db.insp?.IQC,db.iqcRecords,db.inspections?.IQC].find(Array.isArray)||[];}
  function activeIqcRefs(){return new Set(iqcRows().map(row=>{const key=upper(row?.inNo||row?.in_no||row?.lot||row?.lotNo);return key?`IQC:${key}`:'';}).filter(Boolean));}
  function pickPackaging(row){if(!row)return'';for(const key of ['packagingType','packaging_type','packageType','package_type','packType','pack_type','packingType','packing_type','containerType','container_type']){const v=clean(row?.[key]);if(v&&v!=='-'){const other=clean(row?.packagingTypeOther||row?.packaging_type_other||row?.packageTypeOther||row?.package_type_other);return v==='기타'&&other?`${v}(${other})`:v;}}return'';}
  function findIqcByValues(doc,lot,name){const d=upper(doc).replace(/^IQC:/,'');const l=upper(lot);const n=upper(name);return iqcRows().find(row=>{const inNo=upper(row?.inNo||row?.in_no);const rowLot=upper(row?.lot||row?.lotNo||row?.lot_no);const rowName=upper(row?.name||row?.material||row?.item||row?.itemName||row?.item_name);return(d&&inNo&&d===inNo)||(l&&rowLot===l&&(!n||!rowName||n===rowName));})||null;}
  function installInventoryTransactionFilter(){if(window.__QMES_INV_IQC_TX_FILTER_V1__)return;window.__QMES_INV_IQC_TX_FILTER_V1__=true;const originalFetch=window.fetch.bind(window);window.fetch=async function(input,init){const response=await originalFetch(input,init);try{const url=typeof input==='string'?input:String(input?.url||'');const method=String(init?.method||input?.method||'GET').toUpperCase();if(method!=='GET'||!url.includes('/api/inventory/transactions'))return response;const payload=await response.clone().json();if(!payload||payload.success!==true||!Array.isArray(payload.data))return response;const refs=activeIqcRefs();const filtered=payload.data.filter(tx=>{const ref=upper(tx?.reference_no||tx?.referenceNo);return !ref.startsWith('IQC:')||refs.has(ref);}).map(tx=>{if(clean(tx.packaging_type||tx.packagingType))return tx;const row=findIqcByValues(tx.reference_no||tx.referenceNo,tx.lot_no||tx.lotNo,tx.item_name||tx.itemName);const p=pickPackaging(row);return p?{...tx,packaging_type:p}:tx;});const headers=new Headers(response.headers);headers.set('content-type','application/json; charset=utf-8');return new Response(JSON.stringify({...payload,data:filtered}),{status:response.status,statusText:response.statusText,headers});}catch(e){return response;}};}
  function getDetailValue(sheet,label){const cell=Array.from(sheet.querySelectorAll('.inv-tx-detail-grid > div')).find(node=>clean(node.querySelector('dt')?.textContent)===label);return clean(cell?.querySelector('dd')?.textContent)||'-';}
  function makeCell(label,value){const div=document.createElement('div');const dt=document.createElement('dt');const dd=document.createElement('dd');dt.textContent=label;dd.textContent=value||'-';div.append(dt,dd);return div;}
  function formatDate(v){const s=clean(v);return /^\d{4}-\d{2}-\d{2}/.test(s)?s.slice(0,10):(s||'-');}
  function labelProfile(packaging){const p=clean(packaging).toLowerCase();if(/드럼|drum|ibc|tote|탱크/.test(p))return{w:100,h:70,barcode:36,title:14,body:8.5,pad:3.5};if(/말통|pail|캔|can|통|bucket/.test(p))return{w:80,h:60,barcode:30,title:12,body:8,pad:3};if(/포대|bag|sack|봉투|파우치|pouch|박스|box|carton/.test(p))return{w:90,h:60,barcode:30,title:12,body:8,pad:3};if(/병|bottle|소형|vial/.test(p))return{w:70,h:50,barcode:24,title:10,body:7,pad:2.5};return{w:80,h:60,barcode:30,title:12,body:8,pad:3};}
  function adaptivePrintFromDetail(sheet){const packaging=getDetailValue(sheet,'포장형태');const profile=labelProfile(packaging);const values={'원료명':getDetailValue(sheet,'원료명'),'LOT':getDetailValue(sheet,'LOT'),'총 수량':getDetailValue(sheet,'총 수량'),'포장형태':packaging,'입고일자':getDetailValue(sheet,'입고일자'),'검사일자':getDetailValue(sheet,'검사일자'),'이동 방향':getDetailValue(sheet,'이동 방향')};const barcode=sheet.querySelector('.inv-tx-barcode svg');const barcodeHtml=barcode?barcode.outerHTML:'<div class="no-barcode">BARCODE</div>';const win=window.open('','_blank','width=900,height=760');if(!win)return;const rows=[['원료명',values['원료명']],['LOT',values['LOT']],['입고일자',values['입고일자']],['검사일자',values['검사일자']],['포장',values['포장형태']],['수량',values['총 수량']]];const esc=v=>String(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');const gridTop=profile.pad+9,barcodeBottom=1.5,gridBottom=profile.barcode+3.2;win.document.write(`<!doctype html><html><head><meta charset="utf-8"><style>@page{size:${profile.w}mm ${profile.h}mm;margin:0}*{box-sizing:border-box}html,body{margin:0!important;padding:0!important;width:${profile.w}mm!important;height:${profile.h}mm!important;overflow:hidden!important;background:#fff;font-family:Arial,sans-serif}.label{position:relative;width:${profile.w}mm!important;height:${profile.h}mm!important;overflow:hidden!important}.head{position:absolute;left:${profile.pad}mm;right:${profile.pad}mm;top:${profile.pad}mm;height:8mm;display:flex;align-items:center;gap:2mm;border-bottom:.3mm solid #111}.brand-logo{height:6mm;max-width:32mm;object-fit:contain}.material-name{font-size:${profile.title}px;font-weight:900}.grid{position:absolute;left:${profile.pad}mm;right:${profile.pad}mm;top:${gridTop}mm;bottom:${gridBottom}mm;display:grid;grid-template-columns:1fr 1fr;grid-template-rows:repeat(3,1fr)}.cell{padding:.55mm .8mm;border:.2mm solid #ddd;overflow:hidden}.cell small{display:block;font-size:6.5px;color:#555}.cell strong{display:block;margin-top:.2mm;font-size:${profile.body}px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.barcode{position:absolute;left:1.5mm;right:1.5mm;bottom:${barcodeBottom}mm;height:${profile.barcode}mm;display:flex;align-items:center;justify-content:center;overflow:hidden}.barcode svg{width:100%!important;height:${profile.barcode}mm!important;max-width:none!important;max-height:none!important}@media print{html,body,.label{overflow:hidden!important}}</style></head><body><div class="label"><div class="head"><img class="brand-logo" src="${NAMO_LOGO}"><div class="material-name">${esc(values['원료명'])}</div></div><div class="grid">${rows.map(([k,v])=>`<div class="cell"><small>${k}</small><strong>${esc(v)}</strong></div>`).join('')}</div><div class="barcode">${barcodeHtml}</div></div></body></html>`);win.document.close();const doPrint=()=>setTimeout(()=>{win.focus();win.print();setTimeout(()=>win.close(),300);},120);const logo=win.document.querySelector('.brand-logo');if(logo&&!logo.complete){logo.onload=doPrint;logo.onerror=doPrint;setTimeout(doPrint,900);}else doPrint();}
  function fixTransactionDetailLayout(){document.querySelectorAll('.inv-tx-detail-sheet').forEach(sheet=>{const grid=sheet.querySelector('.inv-tx-detail-grid');if(!grid||sheet.dataset.qmesEnterpriseDetail==='1')return;const docNo=getDetailValue(sheet,'문서번호');const material=getDetailValue(sheet,'원료명');const lot=getDetailValue(sheet,'LOT');const type=getDetailValue(sheet,'구분');const qty=getDetailValue(sheet,'총 수량');const packageQty=getDetailValue(sheet,'입고 포장수량');const direction=getDetailValue(sheet,'이동 방향');const operator=getDetailValue(sheet,'작업자');const remark=getDetailValue(sheet,'비고');const linked=findIqcByValues(docNo,lot,material);const packaging=pickPackaging(linked)||getDetailValue(sheet,'포장형태');const recv=formatDate(linked?.recv||linked?.recvDate||linked?.receivedAt);const inspect=formatDate(linked?.inspectedAt||linked?.inspectDate||linked?.inspectionDate);grid.replaceChildren(makeCell('원료명',material),makeCell('구분',type),makeCell('LOT',lot),makeCell('포장형태',packaging),makeCell('입고일자',recv),makeCell('검사일자',inspect),makeCell('총 수량',qty),makeCell('입고 포장수량',packageQty),makeCell('이동 방향',direction),makeCell('작업자',operator),makeCell('비고',remark));const headLead=sheet.querySelector('.inv-tx-detail-head > div');if(headLead){const span=headLead.querySelector('span');if(span){span.innerHTML=`<img src="${NAMO_LOGO}" alt="NAMO Chemical" style="display:block;height:28px;max-width:240px;object-fit:contain;object-position:left center">`;span.style.display='block';span.style.height='28px';}const h3=headLead.querySelector('h3');if(h3)h3.textContent='입출고 처리 상세';}sheet.dataset.qmesEnterpriseDetail='1';});document.querySelectorAll('.inv-tx-detail-actions .primary').forEach(button=>{button.textContent='바코드 인쇄';button.dataset.qmesAdaptivePrint='1';});}
  function installTransactionDetailLayoutFix(){fixTransactionDetailLayout();new MutationObserver(fixTransactionDetailLayout).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',event=>{const button=event.target.closest?.('.inv-tx-detail-actions .primary[data-qmes-adaptive-print="1"]');if(!button)return;const sheet=button.closest('.inv-tx-detail-sheet');if(!sheet)return;event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();adaptivePrintFromDetail(sheet);},true);}
  function restore(){inventorySession++;if(root){try{root.unmount();}catch(e){}root=null;}host?.remove();host=null;const main=document.querySelector('#root>div>main');if(main)Array.from(main.children).forEach(el=>{if(el.dataset.invHidden==='1'){el.style.removeProperty('display');delete el.dataset.invHidden;}});}
  function decorateSidebar(id){if(id!==inventorySession||!host)return;const side=document.getElementById('qmes-sync-sidebar');if(!side)return;const title=side.querySelector('.qmes-side-title'),wrap=side.querySelector('.qmes-side-items');if(title)title.textContent='재고관리';if(!wrap)return;wrap.replaceChildren();sections.forEach(([sid,label])=>{const b=document.createElement('button');b.type='button';b.className='qmes-side-item'+(current===sid?' is-active':'');b.dataset.inventorySection=sid;b.textContent=label;b.onclick=e=>{e.stopPropagation();open(sid);};wrap.appendChild(b);});}
  function inventoryComponent(){if(typeof window.InventoryEnterpriseTab==='function')return window.InventoryEnterpriseTab;try{if(typeof InventoryEnterpriseTab==='function')return InventoryEnterpriseTab;}catch(e){}return null;}
  function open(section='overview'){current=sections.some(s=>s[0]===section)?section:'overview';inventorySession++;const id=inventorySession;const main=document.querySelector('#root>div>main'),Component=inventoryComponent();if(!main||!Component)return;Array.from(main.children).forEach(el=>{if(el!==host){el.dataset.invHidden='1';el.style.setProperty('display','none','important');}});if(!host){host=document.createElement('div');host.id='qmes-inventory-host';main.appendChild(host);}if(root)root.unmount();root=ReactDOM.createRoot(host);root.render(React.createElement(Component,{section:current}));try{sessionStorage.setItem('qmes_inventory_section',current);}catch(e){}if(typeof window.qmesSetGlobalSidebarGroup==='function')window.qmesSetGlobalSidebarGroup('재고관리');setTimeout(()=>decorateSidebar(id),20);setTimeout(()=>decorateSidebar(id),180);}
  function install(){const nav=document.querySelector('.qmes-top-menu');if(!nav||nav.querySelector('[data-qmes-inventory-menu]'))return false;const item=document.createElement('div');item.className='qmes-top-menu-item';item.dataset.qmesInventoryMenu='1';const button=document.createElement('button');button.type='button';button.className='qmes-top-menu-button';button.innerHTML='<span>▣</span><span>재고관리</span><span>›</span>';button.onclick=()=>{let saved='overview';try{saved=sessionStorage.getItem('qmes_inventory_section')||'overview';}catch(e){}open(saved);};item.appendChild(button);const trace=Array.from(nav.children).find(el=>clean(el.textContent).includes('LOT 추적'));if(trace)trace.after(item);else nav.appendChild(item);document.addEventListener('click',event=>{if(!host)return;const top=event.target.closest?.('.qmes-top-menu-button');if(top&&top!==button&&!top.closest('[data-qmes-inventory-menu]'))restore();const sub=event.target.closest?.('.qmes-submenu-button');if(sub)restore();},true);window.qmesOpenInventorySection=open;return true;}
  installInventoryTransactionFilter();installTransactionDetailLayoutFix();const timer=setInterval(()=>{if(install())clearInterval(timer);},250);setTimeout(()=>clearInterval(timer),15000);
})();
